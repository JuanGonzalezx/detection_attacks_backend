[project]
name = "detection-attacks-whatsapp"
version = "0.1.0"
description = "Orquestador WhatsApp Cloud API -> Lambda (Rekognition) para deteccion de fraude en cedulas"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.32",
    "httpx>=0.27",
    "asyncpg>=0.30",
    "pydantic>=2.9",
    "pydantic-settings>=2.6",
    "python-dotenv>=1.0",
]

[tool.uv]
package = false
